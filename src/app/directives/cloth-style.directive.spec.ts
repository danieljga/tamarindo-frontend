import { ClothStyleDirective } from './cloth-style.directive';

describe('ClothStyleDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ClothStyleDirective();
  //   expect(directive).toBeTruthy();
  // });
});
